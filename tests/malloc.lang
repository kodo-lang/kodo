// extern from libc.
// TODO: free/putchar return void.
extern fn malloc(size: i64) => *i32;
extern fn free(ptr: *i32) => i8;
extern fn putchar(character: i32) => i32;

fn main() => i32 {
    var mem: *i32 = malloc(1);
    *mem = 65;
    putchar(*mem);
    free(mem);
    return 0;
}
